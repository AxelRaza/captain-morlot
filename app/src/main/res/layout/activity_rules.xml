<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".RulesActivity">

    <ImageButton
            android:id="@+id/return_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_back"
            android:layout_marginTop="10dp"
            android:layout_marginRight="60dp"
            android:layout_toLeftOf="@id/rules_title_textview"
            android:onClick="backToHome"
    />

    <TextView android:id="@+id/rules_title_textview"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_marginTop="50dp"
              android:layout_centerHorizontal="true"
              android:textSize="25dp"
              android:text="Règles du jeu"
    />

    <TextView
            android:id="@+id/rules_desc_textview"
            android:layout_width="350dp"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/rules_title_textview"
            android:layout_marginTop="50dp"
            android:layout_marginBottom="50dp"
            android:textAlignment="center"
            android:textSize="18dp"
            android:text="Des matelots, un pirate et un moussaillon suffisent pour semer la pagaille à bord !
    Le but du jeu est de gagner en tant que pirate ! \n
    Un mot de départ va être donné.la première personne devra dire un mot en relation avec le mot donné (suite, contraire, homophones, description, selon votre rôle)
    La personne suivante devra faire de même et ainsi de suite.
    "/>

    <android.support.v7.widget.RecyclerView
            android:layout_width="350dp"
            android:layout_height="250dp"
            android:id="@+id/roles_list"
            android:layout_below="@id/rules_desc_textview"
            android:layout_centerHorizontal="true" />

</RelativeLayout>